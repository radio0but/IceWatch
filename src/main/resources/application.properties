spring.application.name=radio-backend
server.port=9090

# ==============================================================================
# 🎛️  ICEWATCH – CONFIGURATION PRINCIPALE
# ==============================================================================

# Jeton maître pour accéder à toutes les ressources protégées (à remplacer !)
icewatch.master-token=CHANGER_MOI

# Domaine autorisé pour l'obtention de jetons (doit correspondre au Referer)
icewatch.allowed-domain=https://votre-domaine-frontend.com

# URL interne du serveur Owncast (ne pas exposer publiquement)
icewatch.owncast-url=http://localhost:8123

# URL interne du flux Icecast
icewatch.icecast-stream-url=http://localhost:8000/radio


# ==============================================================================
# 🔐  AUTHENTIFICATION LDAP (FACULTATIVE)
# ==============================================================================
# Ces paramètres activent l’authentification via un annuaire LDAP externe.
# Commentez-les pour utiliser uniquement les comptes locaux.

#spring.ldap.urls=ldap://ldap.exemple.local:389
#spring.ldap.base=dc=exemple,dc=local
#spring.ldap.username=cn=admin,dc=exemple,dc=local
#spring.ldap.password=CHANGER_MOI
#spring.ldap.user-dn-patterns=uid={0},ou=Users
#spring.security.ldap.authorities.group-search-base=ou=Groups
#spring.ldap.embedded.base-dn=dc=exemple,dc=local


# ==============================================================================
# 📴  DÉSACTIVER LA CONNEXION UTILISATEUR (MODE PUBLIC)
# ==============================================================================
# Activez cette option pour désactiver complètement le système de login (mode kiosque ou accès anonyme)
# icewatch.disable-login=true


# ==============================================================================
# 🚫  LIMITATION DES TENTATIVES DE CONNEXION – LoginRateLimitFilter
# ==============================================================================
# Ces propriétés sont facultatives.
# Si elles ne sont pas définies, les valeurs par défaut suivantes s’appliquent :
#    → 5 tentatives autorisées
#    → Blocage pendant 10 minutes après dépassement

# Nombre maximum de tentatives de connexion (formulaire POST /login)
# icewatch.login.max-attempts=5

# Durée de blocage (en minutes) après dépassement du quota
# icewatch.login.block-minutes=10


# ==============================================================================
# 🪵  LOGGING (FACULTATIF – POUR DEBUG)
# ==============================================================================
# Activez ces options pour afficher plus de détails dans les logs backend.
#logging.level.org.springframework.web=DEBUG
#logging.level.org.springframework.security=DEBUG
